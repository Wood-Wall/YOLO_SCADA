# 数据集配置文件
# 用法: yolo train model=yolo11n.pt data=dataset.yaml epochs=100

# 数据集根目录
path: D:/CodeExa/langchain/YOLO/datasets/bvn

# 图片目录（相对于 path）
train: images/train
val: images/val

# 类别数量
nc: 2

# 类别名称（顺序和 classes.txt 一致）
names:
  0: helicopter
  1: person